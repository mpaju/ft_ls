#include "ft_ls.h"
#include "libft/libft.h"

const char	*find_args(int ac, const char **av)
{
	char	*path;
	int		flags;

	flags = 0;
	while (*av)
	{
		if (*av[0] == '-')
			flag_process
	
	}
	return (av[1]);
}


